# Developer Site

Tutorials and discussion posts on various developer technologies.

## Setup

After cloning project, install dependencies:

```sh
npm i
```

Start a development server:

```sh
npm run dev

# Or start the server and open the app in a new browser tab
npm run dev -- --open
```

To run the codebase formatter:

```zsh
npm run format
```

## Building

To create a production version of your app:

```sh
npm run build
```

You can preview the production build with `npm run preview`.

<!--> App deployment may require installing an [adapter](https://svelte.dev/docs/kit/adapters) for the target environment-->
